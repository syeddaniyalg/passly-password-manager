apiVersion: v1
kind: Service
metadata:
  name: passly-backend-service
  namespace: passly
spec:
  selector:
    app: passly-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
